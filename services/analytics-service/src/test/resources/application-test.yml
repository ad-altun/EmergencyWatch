spring:
  application:
    name: analytics-service

  # H2 In-Memory Database for Testing
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  # JPA/Hibernate Configuration for H2
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  # Embedded MongoDB for Testing (auto-configured by Flapdoodle)
  data:
    mongodb:
      database: emergencywatch_analytics_test

  # Kafka Configuration - will use embedded Kafka from spring-kafka-test
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers:localhost:9092}
    consumer:
      group-id: analytics-service-test-group
      auto-offset-reset: earliest

  jackson:
    serialization:
      write-dates-as-timestamps: false

# Embedded MongoDB Configuration
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.14

server:
  port: 0  # Random port for tests

logging:
  level:
    de.denizaltun.analyticsservice: INFO
    org.springframework.kafka: WARN
    de.flapdoodle.embed.mongo: WARN
